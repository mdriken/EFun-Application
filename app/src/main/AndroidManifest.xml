<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.riken.efun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".pilih_kategori" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".BuahActivity" />
        <activity android:name=".BendaActivity" />
        <activity android:name=".HewanActivity" />
        <activity android:name=".apelActivity" />
        <activity android:name=".grapesActivity" />
        <activity android:name=".HewanActivity2" />
        <activity android:name=".BuahActivity1" />
        <activity android:name=".BendaActivity1"></activity>
    </application>

</manifest>